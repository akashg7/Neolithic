"""Demands router — stub for Nilesh to implement."""
from fastapi import APIRouter

router = APIRouter()


# TODO: Nilesh implements these endpoints
# POST /demands — create buyer demand
# GET /demands — list buyer's own demands
# GET /demands/{id}/matches — multi-lot matching
