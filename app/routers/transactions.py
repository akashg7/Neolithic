"""Transactions router — stub for Nilesh to implement."""
from fastapi import APIRouter

router = APIRouter()


# TODO: Nilesh implements these endpoints
# POST /transactions/{offer_id}/create-order — Razorpay order creation
# POST /transactions/webhook — Razorpay webhook handler
# GET /transactions/{id} — transaction status + events
