"""Voice & Calls router — Tier 3 stretch. Stub for Nikhil/Nilesh."""
from fastapi import APIRouter

router = APIRouter()


# TODO: Tier 3 — only build after Day 2 Hour 5 freeze
# POST /voice/narrate — TTS via Bhashini
# POST /calls/initiate — Agora/ZegoCloud call session
# PATCH /calls/{id}/end — end call session
