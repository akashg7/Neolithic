"""Disputes router — stub for Nilesh to implement."""
from fastapi import APIRouter

router = APIRouter()


# TODO: Nilesh implements these endpoints
# POST /disputes — raise a dispute
# GET /disputes — list user's disputes
# PATCH /disputes/{id} — update dispute status
